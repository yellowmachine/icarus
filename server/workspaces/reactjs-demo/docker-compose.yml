services:
  code: 
    build: .
    ports: 
      - 8080:8080
    #volumes:
    #   - "$HOME/.config:/root/.config"
    #   - ~/.ssh:/root/.ssh
    command:
      - /bin/sh
      - -c
      - |
        git clone https://github.com/yellowmachine/reactjs-demo.git .
        code-server /project
